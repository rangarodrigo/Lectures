

% Coded by Ranga Rodrigo based on Signals and Systems by Alan V Oppenheim.
% D.

clear;
close all;



N1 = 2; 
omega = -3*pi:pi/100:3*pi;



stem(n, xn, 'o', 'MarkerFaceColor','k','MarkerSize',5)
title(['N = ' num2str(N) ', \omega_0 = 2\pi/' num2str(N) ': Periodic'])

fn = '../Lectures/b_signals/figures/dt_sinusoid_n_12.dat';
fileID = fopen(fn,'w');
fprintf(fileID,'%s\t%s\n','n','xn');
fclose(fileID);
dlmwrite(fn, [n', xn'],'delimiter', '\t', 'precision', 6 , '-append');


